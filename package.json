{
  "name": "dr-anil-raheja",
  "version": "1.0.0",
  "private": true,
  "description": "Static export of dranilraheja.com — orthopedic surgeon site in Next.js Pages Router with interactive patient tools. See DESIGN_TASTE.md.",
  "scripts": {
    "dev": "node scripts/generate-nav.mjs && next dev",
    "build": "node scripts/generate-nav.mjs && next build",
    "start": "python3 -m http.server 3000 --directory out",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "14.2.15",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5"
  }
}
